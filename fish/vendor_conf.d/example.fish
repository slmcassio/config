# Example private conf.d file
# Copy to config-private/fish/vendor_conf.d/ and rename with a number prefix.
# Fish loads these alongside ~/.config/fish/conf.d/, sorted by filename.
#
# Example: 35-work-env.fish loads between public 30-fzf.fish and 40-gpg.fish.

# set -gx WORK_API_URL https://api.example.com
# set -gx WORK_TEAM my-team
# abbr -a vpn 'sudo openconnect vpn.example.com'
